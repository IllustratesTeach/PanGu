INCLUDE_DIRECTORIES(../src)
SET(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE}" )

ADD_EXECUTABLE(test_wsq test_wsq.cc)
TARGET_LINK_LIBRARIES(test_wsq ${GTEST_BOTH_LIBRARIES} hall-image4j)

ADD_TEST(TestWsq test_wsq)

